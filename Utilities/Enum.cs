using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum JobType
{ build, carry, farm, cook, care, produce, gather, guard, maintain};

public enum NeedType
{ Hunger, Energy, Happiness};

public enum Faction
{ player, village_0, invader, guerilla, puppet, town_1, town_2, village_1, village_2, village_3, village_4, village_5 };

public enum BuildMode
{ floor, structure, furniture, area, destroy };

public enum ArchStats
{ building, destroying, dismantling, done};

public enum ScheduleType
{ Work, Free, Rest};

